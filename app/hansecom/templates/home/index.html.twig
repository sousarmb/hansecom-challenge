{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    .quotes-wrapper  { display: flex; flex-direction: row; align-items: center; justify-content: center; }
</style>

<div class="example-wrapper">
    <h1>{{ 'Hello {user}'|trans({'{user}':app.user.email}) }}! ✅</h1>
    <h2>{{ 'request_a_quote'|trans }}</h2>
    <div>
        <input id="symbol" type="text" placeholder="{{ "symbol"|trans }}">
        <button id="request_quote">{{ "request_quote"|trans }}</button>
    </div>
    
    <hr>
</div>
<div class="example-wrapper">
    {% include 'quotes/grid.html.twig' %}
</div>

{% endblock %}
